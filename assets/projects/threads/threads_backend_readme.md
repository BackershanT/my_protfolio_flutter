# Threads Backend

A backend API for a Threads clothing store built with Node.js and Express. The system handles user authentication, product management with variants (size, color, fabric), order processing, and payment integration with Razorpay. It includes both user-facing and admin functionalities with role-based access control.